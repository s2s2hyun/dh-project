---
title: '프론트엔드 CSS 지식 정리 1탄'
description: '"프런트 엔드 CSS 지식 요약 1부.MDN에 작성된 내용을 보며 정리 내용'
image: '../../public/blogs/kelly-sikkema--1_RZL8BGBM-unsplash.jpg'
publishedAt: '2023-11-14'
updatedAt: '2023-11-14'
author: 'DH'
isPublished: true
tags:
  - css
  - HTML
---

CSS는 Cascading Style Sheets의 약자로 웹 페이지의 스타일을 지정하는 언어이다. CSS를 통해 HTML에 스타일을 지정할 수 있다.
프론트의 장벽 중 하나이기도 하다. 이를 MDN 튜토리얼을 통해 공부해봤습니다.

## 1. CSS 란?

CSS는 웹사이트의 스타일 가이드와 같습니다. 편지를 쓴다고 상상해보면, HTML은 단어나 단락과
같은 편지 내용을 작성하는 데 사용되는 반면,CSS는 보기 좋게 만들기 위해 글꼴, 색상,
레이앗을 선택하는 것과 같습니다. 웹사이트에 색상, 글꼴, 간격 등을 표시하는 방법을 알려주어
웹 사이트를 보기 좋고 체계적으로 보이게 만듭니다. CSS는 사이트의 기본 구조를 제공하는
HTML과 함께 작동하여 웹에서 시각적으로 매력적이고 사용자에게 친화적인 환경을 만들어줍니다.

### 1.1 CSS 구문

CSS는 selector를 통해 특정 요소나 요소의 그룹을 선택하고 그 요소에 스타일을 적용한다. 이때 selector는 요소의 이름이나 클래스, 아이디 등을 사용할 수 있다.
그리고 스타일은 속성과 값으로 구성된다. 예를 들어 다음과 같다.

```html
h1 { color: blue; font-size: 1rem; }
```

h1 요소들을 선택하고 속성:값; 형태로 스타일을 지정했다. 이때 각 스타일의 구분은 ;로 한다.
이때 이 속성은 CSS 프로퍼티라고 하는데 각 프로퍼티에 따라 허용되는 값이 있다. color 프로퍼티에는 색상만 올 수 있는 식이다.

### 1.2 CSS 사양

CSS는 W3C의 CSS 작업 그룹에 의해 개발되며, 새로운 기능은 항상 과거 버전과의 호환성을 고려하여 개발됩니다.
이러한 접근 방식은 오래된 웹사이트가 현대 브라우저에서도 잘 작동하도록 보장합니다.

### 1.3 브라우저 지원

브라우저에 따라서 CSS를 지원하는 정도가 다르다. 브라우저에 구현되지 않은 CSS를 사용하면 화면에 출력되지 않는다. 최신 사양의 경우 브라우저에 따라 구현된 브라우저와 안 된 브라우저가 있습니다.

이렇게 브라우저가 특정 CSS를 구현했는지를 caniuse.com에서 확인할 수 있다. 예를 들어서 grid를 검색하면 다음과 같이 나온다.

<Image
  src="/blogs/mdn-css/flex-search.png"
  width="1000"
  height="600"
  alt="Image"
  sizes="100vw"
/>

웹사이트 "caniuse.com"은 웹 개발자와 디자이너가 다양한 브라우저와 플랫폼에서 다양한 웹 기술의 호환성을 확인할 수 있는 귀중한 리소스입니다.

[caniuse.com flex 검색](https://caniuse.com/?search=flex).

## 2.Css 시작하기

CSS는 HTML 문서에 적용된다. HTML 문서에 CSS를 적용하는 방법은 여러 가지가 있는데 가장 일반적인 방법으로 해보자. HTML 문서에 CSS를 연결하는 방법이다.

CSS 파일을 HTML 문서와 같은 위치에 만들고 HTML 문서의 head 태그 내에 다음을 추가하자. CSS 파일 이름은 index.css라 하자.

```index.html

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <link href="index.css" rel="stylesheet" type="text/css" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Vite + React + TS</title>
  </head>
  <body>
      <h1>HTML CSS 시작하기~</h1>
  </body>
</html>

```

이제 CSS 파일에 스타일을 추가해보자. 다음과 같이 하면 된다.

```index.css
h1 {
  font-size: 3.2em;
  line-height: 1.1;
}
```

<Image
  src="/blogs/mdn-css/render.png"
  width="1000"
  height="600"
  alt="Image"
  sizes="100vw"
/>

css 가 적용된 폰트의 모습을 보실수 있습니다.

### 2.1. 여러 가지 셀렉터

#### 2.1.1. 전체 셀렉터

\*을 사용하면 전체 요소를 선택할 수 있다.

```index.css
* {
  font-size: 2rem;
}
```

#### 2.1.2. id 셀렉터

id 셀렉터는 #id와 같이 #을 사용한다. 문서 전체에서 특정 id를 가진 요소는 하나뿐이어야 하므로 id 셀렉터는 하나의 요소만 선택할 수 있다.

```index.css
index.html
<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" type="text/css" href="styles.css">
</head>
<body>
    <p id="title">Title</p>
</body>
</html>


#title {
  color: blue;
}
```

#### 2.1.3. class 셀렉터

HTML 요소 전체를 선택하여 스타일링하는 것은 한계가 많다. 클래스를 사용하면 된다. CSS에서 .클래스명과 같이 온점을 쓰면 클래스를 선택하여 스타일링할 수 있다.

```index.css
index.html
<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" type="text/css" href="styles.css">
</head>
<body>
    <p class="text-style">Text Style은</p>
    <div class="text-style">붉은색 , 크기는 20px</div>
</body>
</html>


.text-style {
    color: red;
    font-size: 20px;
}
```

HTML 파일에서 p 및 div 요소 모두 text-style 클래스에 할당되었습니다.
CSS 파일에서 .text-style 클래스 선택기는 text-style 클래스가 있는 모든 요소를 ​​대상으로 하며 지정된 스타일을 해당 요소에 적용합니다.

### 2.1.4. 속성 셀렉터

특정 속성을 가진 요소를 선택할 수 있다. 셀렉터[속성]과 같이 쓰면 그 셀렉터가 선택한 요소 중 특정 속성을 가진 요소를 선택할 수 있다. 다음과 같이 쓰면 a태그 중 target 속성을 가진 요소만 선택할 수 있다.

```index.css
<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" type="text/css" href="index.css">
</head>
<body>
    <a href="https://example.com" target="_blank">예제 링크</a>
    <a href="https://anotherexample.com">다른 링크</a>
</body>
</html>

index.css
a[target="_blank"] {
    color: red;
}
```

- HTML 파일에는 현재 a 태그가 2개가 있지만 , 그중 하나만 "target="\_blank"" 속성을 가지고 있습니다.
- CSS 파일에는 속성 선택자 "a[target="_blank"]" 이 포함되어 있습니다. 이 선택자는 target="\_blank" 속성을 가진 a 태그만을 대상으로 합니다.
- 선택된 요소는 빨간색으로 스타일링되므로, '예제 링크'는 다른 탭에서 열리는 링크와 구분되어 빨간색으로 표시됩니다."
